<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Attendance Data</title>
    <link rel="icon" type="image/x-icon" href="./abc.png" />
    <link href="https://getbootstrap.com/docs/5.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <style>
        body {
            background-color: black;
            color: white;
        }
        .card-header {
            background-color: black;
            color: white;
        }
        .table {
            color: black;
            border: 1px solid #444;
            background-color: white; /* Ensure table background is white */
            width: 100%;
        }
        th, td {
            background-color: white; /* Ensure cells background is white */
            color: black;
            border: 1px solid #444;
            padding: 10px;
            position: relative;
            width: 50px; /* Set the width of each cell */
            text-align: center; /* Center-align content */
        }
        th {
            background-color: #ffffff; /* Ensure header cells background is white */
        }
        .table-striped tbody tr:nth-of-type(odd) {
            background-color: white; /* Ensure odd rows background is white */
        }
        .table-striped tbody tr:nth-of-type(even) {
            background-color: white; /* Ensure even rows background is white */
        }
        .table-striped tbody tr {
            border-bottom: 1px solid #ddd; /* Subtle separator between rows */
        }
        .table-striped td:not(:last-child) {
            border-right: 1px solid #ddd; /* Subtle separator between columns */
        }
        .editable input {
            background-color: white;
            color: black;
            border: 1px solid #ccc;
            text-align: center; /* Center-align input content */
        }
        .save-btn, .cancel-btn {
            cursor: pointer;
            padding: 5px 10px;
            border-radius: 5px;
            border: 1px solid transparent;
            display: inline-block;
            margin: 0 5px;
            color: white;
            background-color: black;
            transition: background-color 0.3s, border-color 0.3s;
            text-align: center; /* Center-align button content */
        }
        .save-btn:hover {
            background-color: #28a745;
            border-color: #28a745;
        }
        .cancel-btn:hover {
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .save-btn, .cancel-btn {
            border-color: black;
        }
        h1 {
            color: white;
            text-align: center;
            margin-bottom: 30px;
        }
        .actions {
            text-align: center;
        }
        @media (max-width: 768px) {
            .table {
                font-size: 24px; /* Increase font size for better readability */
            }
            th, td {
                font-size: 24px; /* Increase font size for better readability */
                padding: 15px; /* Increase padding for touch friendliness */
            }
            .save-btn, .cancel-btn {
                padding: 10px 20px; /* Larger padding for better touch targets */
                font-size: 18px; /* Increase font size for better readability */
            }
            h1 img {
                width: 50px; /* Adjust image size for smaller screens */
                height: 50px;
            }
            .card-header {
                font-size: 20px; /* Adjust font size for the card header */
            }
            /* Make the table scrollable horizontally on small screens */
            .table-responsive {
                overflow-x: auto;
                -webkit-overflow-scrolling: touch; /* Enable smooth scrolling */
            }
        }
    </style>
</head>
<body>
    <div class="container mt-5 mb-5">
        <h1>
            <img src="abc.png" alt="Girl in a jacket" width="70" height="70" style="margin-top: -8px;">
            <b>Student Attendance Data</b>
        </h1>
        <div class="mt-3 mb-3">
            <div class="card">
                <div class="card-header" style="font-size: 18px;">
                    Students Data table
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped mt-3">
                            <thead>
                                <tr>
                                    <th>Field</th>
                                    <% data.forEach(function(row) { %>
                                        <th>Record</th>
                                    <% }); %>
                                </tr>
                            </thead>
                            <tbody>
                                <% columns.forEach(function(column) { %>
                                    <tr>
                                        <td><%= column %></td>
                                        <% data.forEach(function(row) { %>
                                            <td class="editable" data-column="<%= column %>"><%= row[column] %></td>
                                        <% }); %>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</body>
</html>
